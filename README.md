# Deep Q-Learning for OpenAI environments

## Train

Run 'main.py' to setup training parameters. The final model is saved in 'models/'.

## Test

Run 'test.py' to load an already trained model and observe how the model performs.
